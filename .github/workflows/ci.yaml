name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build examples — ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            preset: windows
          - os: macos-latest
            preset: macos

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      # ── Configure ────────────────────────────────────────────────────────
      - name: Configure (Linux / macOS)
        if: runner.os != 'Windows'
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: |
          cmake -S . -B build `
            -DCMAKE_BUILD_TYPE=Release

      # ── Build ────────────────────────────────────────────────────────────
      - name: Build
        run: cmake --build build --config Release --parallel

      # ── Verify every example binary exists ──────────────────────────────
      - name: Verify example binaries (Linux / macOS)
        if: runner.os != 'Windows'
        run: |
          for example in breakout gravitas gravitas_ai molds steering_behavior thetower predator_and_prey; do
            bin="build/examples/${example}/${example}"
            if [ ! -f "$bin" ]; then
              echo "❌ Missing binary: $bin"
              exit 1
            fi
            echo "✅ $bin"
          done

      - name: Verify example binaries (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $examples = @("breakout","gravitas","gravitas_ai","molds","steering_behavior","thetower","predator_and_prey")
          foreach ($ex in $examples) {
            $bin = "build\examples\$ex\Release\$ex.exe"
            if (-Not (Test-Path $bin)) {
              Write-Error "Missing binary: $bin"
              exit 1
            }
            Write-Host "✅ $bin"
          }
